class Pathfinder:
    """the whole thing"""
    def __init__(self, drones: list, nodes: list, connections: list)
        """Initialize"""
        self.drones = drones
        self.nodes = nodes
        self.connections = connections
        self.paths = paths[]
        self.algorithm(drones, nodes, connections, paths)
    
    def algorithm()
        """logic and assigning"""
        dict cost_paths = {}
            